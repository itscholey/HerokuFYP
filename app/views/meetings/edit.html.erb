<% provide(:title, "Edit Meeting") %>
<% provide(:button_text, "Save Changes") %>
<% provide(:pagetitle, "Edit Meeting: #{@meeting.subject}") %>

<%= link_to "Back to Meeting: #{@meeting.subject}", @meeting, class: "btn btn-primary task-button" %>

<%= render "form" %>
