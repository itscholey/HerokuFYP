<% provide(:title, "Enrolments") %>
<h1> Enrolments </h1>

<% Enrolment.all.each do |e| %>
  <div class="enrolment_class">
    <h3><%= e.user_id %>, <%= User.find(e.user_id).name %></h3>
    <h3><%= e.subject_id %>, <%= Subject.find(e.subject_id).name %></h3>
  </div>
<% end %>
