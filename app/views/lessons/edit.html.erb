<% provide(:title, "Edit #{@lesson.name}") %>
<% provide(:button_text, "Save Changes") %>
<% provide(:view_class, "edit_lesson") %>
<% provide(:pagetitle, "Edit #{@lesson.name}") %>

<%= link_to "Back to #{ @lesson.name}", @lesson, class: "btn btn-primary task-button" %>

<%= render "form" %>
