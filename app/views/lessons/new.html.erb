<% provide(:title, "Create Lesson") %>
<% provide(:button_text, "Create Lesson") %>
<% provide(:view_class, "new_lesson") %>
<% provide(:pagetitle, "Create a New Lesson for #{Subject.find(session[:subject_id]).name}") %>

<%= link_to "Back to #{ Subject.find(session[:subject_id]).name}", Subject.find(session[:subject_id]), class: "btn btn-primary task-button" %>

<%= render "form" %>
