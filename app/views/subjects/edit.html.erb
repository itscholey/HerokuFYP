<% provide(:title, "Edit Subject") %>
<% provide(:button_text, "Save Changes") %>
<% provide(:view_class, "edit_subject") %>
<% provide(:pagetitle, "Edit #{@subject.name}") %>

<%= link_to "Back to #{@subject.name}", @subject, class: "btn btn-primary task-button" %>

<%= render "form" %>
